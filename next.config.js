/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'f5j5plauetav5yal.public.blob.vercel-storage.com'],
    unoptimized: true,
  },
}

module.exports = nextConfig